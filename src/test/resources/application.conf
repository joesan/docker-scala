# default test docker client configuration
docker {
  server {
    uri="https://localhost:2376"
    version=""
    userName="${user.name}"
    password=""
    serverAddress="https://index.docker.io/v1/"
  }
  io {
    email=""
    dockerCertPath=""
    readTimeOut=0
  }
}